import React, { useState, useEffect } from 'react';
import { Answer } from '../types'

type Props = { answers: Answer[] };

export const QuizResult: React.FC<Props> = ({ answers }) => {
  return (
    <div></div>
  )
}

